-- GuildJoinPrompt.lua
-- Guild-join prompting is now handled by SetupWizard.lua.
-- This file is kept as a stub so existing TOC references don't break.
-- It will be removed in a future cleanup once the wizard is stable.

function SchlingelInc:CheckAndShowGuildJoinPrompt()
    -- Intentionally empty — wizard handles this step now.
end
